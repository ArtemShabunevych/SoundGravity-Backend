================================================================================
SOUNDGRAVITY — ПОВНИЙ ПЛАН ЗМІН (БЕКЕНД + ФРОНТЕНД)
================================================================================
Дата: 17.06.2026
Фронтенд: https://github.com/ArtemShabunevych/SoundGravity-Frontend.git
Бекенд:   C:\Users\Admin\WebstormProjects\backend

Умовні позначення:
  [x] = ВЖЕ ЗРОБЛЕНО
  [ ] = ТРЕБА ЗРОБИТИ


================================================================================
1. БЕКЕНД — ВЖЕ ЗРОБЛЕНО (НЕ ЧІПАТИ)
================================================================================

[x] 1.1. User entity — avatarUrl
      Файл: src/modules/users/entities/user.entity.ts:28
      @Column({ nullable: true }) avatarUrl: string;

[x] 1.2. Track entity — visibility
      Файл: src/modules/tracks/entities/track.entity.ts:41-45

[x] 1.3. Playlist entity — visibility
      Файл: src/modules/playlists/entities/playlist.entity.ts:40-45

[x] 1.4. PATCH /api/users/avatar
      Файл: users.controller.ts:62-80
      Приймає як файл (FileInterceptor), так і base64 string.

[x] 1.5. updateAvatar в UsersService
      Файл: users.service.ts:105-116
      Використовує CloudinaryService для завантаження.

[x] 1.6. userId береться з JWT, не з body
      tracks.controller.ts:42 — req.user.userId
      CreateTrackDto НЕ містить userId.

[x] 1.7. POST /api/playlists/:id/like
      playlists.controller.ts — toggleLike

[x] 1.8. POST /api/playlists/:id/cover
      playlists.controller.ts — uploadCover (файл + base64)

[x] 1.9. PATCH /api/playlists/:id
      playlists.controller.ts — update (з перевіркою власника)

[x] 1.10. DELETE /api/playlists/:id
      playlists.controller.ts — remove (з перевіркою власника)

[x] 1.11. GET /api/playlists/:id/like-status
      playlists.controller.ts — getLikeStatus

[x] 1.12. GET /api/tracks/:id/like-status
      tracks.controller.ts — getLikeStatus

[x] 1.13. @UseGuards(JwtAuthGuard) на GET /users та GET /users/:id
      users.controller.ts:25, 50

[x] 1.14. password виключено з findOne
      users.service.ts:37 — деструктуризація

[x] 1.15. CreatePlaylistDto — @IsOptional() для coverUrl, genre, description
      create-playlist.dto.ts:9-18

[x] 1.16. synchronize через env var DB_SYNCHRONIZE
      database.config.ts:19

[x] 1.17. .env.example — прибрано JWT_ACCESS_SECRET, додано DB_SYNCHRONIZE


================================================================================
2. БЕКЕНД — ТРЕБА ДОДАТИ (8 ПУНКТІВ)
================================================================================

[ ] 2.1. GET /api/tracks/user/:username
      Де викликається: фронтенд User.tsx:66
      Опис: публічні треки користувача за username
      Файли:
        - tracks.controller.ts — новий @Get('user/:username')
        - tracks.service.ts — новий метод findByUsername(username)
      Важливо: повертати тільки visibility === PUBLIC

[ ] 2.2. GET /api/playlists/user/:username
      Де викликається: фронтенд User.tsx:86
      Опис: публічні плейлісти користувача за username
      Файли:
        - playlists.controller.ts — новий @Get('user/:username')
        - playlists.service.ts — новий метод findByUsername(username)

[ ] 2.3. GET /api/tracks/pagination?limit=&cursor=
      Де викликається: фронтенд StoriesService.js:4
      Опис: cursor-based пагінація треків
      Файли:
        - tracks.controller.ts — новий @Get('pagination')
        - tracks.service.ts — новий метод findWithPagination()
      Формат відповіді: { tracks: Track[], nextCursor: string | null }

[ ] 2.4. GET /api/users/me — додати id та avatarUrl
      Файл: users.service.ts — getMyProfile()
      Поточна відповідь: { username, createdAt }
      Потрібно: { id, username, avatarUrl, createdAt }

[ ] 2.5. GET /api/playlists/:id — додати user в relations
      Файл: playlists.service.ts — findOneWithTracks()
      Поточно: relations: { tracks: true }
      Потрібно: relations: { tracks: true, user: true }
      Фронтенд Playlist.tsx:76 — виводить playlist.user.username

[ ] 2.6. POST /api/likes/track/:trackId — додати likesCount у відповідь
      Файл: likes.service.ts — toggleTrackLike()
      Поточна відповідь: { liked: true/false, type: 'track' }
      Потрібно: { liked, type, likesCount }

[ ] 2.7. POST /api/likes/playlist/:playlistId — додати likesCount у відповідь
      Файл: likes.service.ts — togglePlaylistLike()
      Поточна відповідь: { liked: true/false, type: 'playlist' }
      Потрібно: { liked, type, likesCount }

[ ] 2.8. Узгодити неймінг cover
      Поточно: POST /api/playlists/:id/cover
      В твоєму плані: PATCH /api/playlists/:id/cover
      Треба вибрати один варіант і зафіксувати.


================================================================================
3. ФРОНТЕНД — ЩО ТРЕБА ЗМІНИТИ
================================================================================

[ ] 3.1. Аватарка — прибрати localStorage костиль
      Файл: src/context/UserContext.jsx
        - Прибрати читання localStorage.getItem("USER_AVATAR_URL")
        - Після фіксу 2.4 (бекенд повертає avatarUrl) дані будуть з сервера
      Файл: src/components/User/User.tsx
        - Прибрати localStorage.getItem("USER_AVATAR_URL")
        - Прибрати localStorage.setItem("USER_AVATAR_URL", ...)

[ ] 3.2. CreateTrack — додати refresh-логіку в XHR
      Файл: src/components/CreateTrack/CreateTrack.tsx
      Проблема: XHR (рядки 81-112) не має обробки 401 + refresh
      Рішення: перед XHR перевірити токен через fetchWithAuth
      АБО додати в XHR обробку 401 + виклик /auth/refresh

[ ] 3.3. CreatePlaylist — переробити під бекенд
      Файл: src/components/CreatePlaylist/CreatePlaylist.tsx
      Варіант А: один multipart POST (як CreateTrack) з cover в FormData + XHR onprogress
      Варіант Б: JSON POST + після створення окремий запит на cover через PATCH/:id/cover

[ ] 3.4. Лайки — викликати like-status при завантаженні
      Файл: src/components/Track/Track.tsx
        - Викликати GET /api/tracks/:id/like-status в useEffect
        - Встановити isLiked з відповіді сервера
      Файл: src/components/Playlist/Playlist.tsx
        - Аналогічно для плейліста

[ ] 3.5. Прогрес бар — використовувати XHR onprogress
      Файл: src/components/CreateTrack/CreateTrack.tsx
      Проблема: fetch не дає upload.onprogress
      Рішення: використовувати XMLHttpRequest.upload.onprogress
      Вже є в коді (XHR), але без refresh-логіки (див. 3.2)

[ ] 3.6. PlaylistsList.tsx — перевірити чи працює з GET /api/playlists
      Поточний бекенд: GET /api/playlists повертає тільки плейлісти поточного юзера
      Фронтенд: PlaylistsList.tsx — очікує СПИСОК ВСІХ плейлістів
      Потрібно або:
        - Додати новий ендпоінт GET /api/playlists/public (вже є)
        - Або переробити GET /api/playlists на всі публічні

[ ] 3.7. AuthForm.tsx — перевірити коректність полів
      POST /auth/register приймає: { email, password, username }
      POST /auth/login приймає: { email, password }
      Переконатись що фронтенд відправляє саме ці поля.

[ ] 3.8. fetchWithAuth — не передавати Content-Type для FormData
      Файл: src/API/apiClient.js
      Якщо options.body є FormData, браузер сам встановлює multipart boundary.
      Поточний код не має ручного Content-Type — має працювати.


================================================================================
4. ПОРЯДОК ВИКОНАННЯ
================================================================================

Крок 1 — Бекенд (нові ендпоїнти):
  2.1 → 2.2 → 2.3 (нові GET)
  2.4 (users/me) → 2.5 (playlists/:id user relation)
  2.6 → 2.7 (likesCount)
  2.8 (вирішити неймінг)

Крок 2 — Фронтенд (після бекенду):
  3.1 (аватарка) — після 2.4
  3.4 (лайки) — після 2.6/2.7
  3.3 (CreatePlaylist) — після 2.8
  3.2 (CreateTrack XHR refresh)
  3.5 (прогрес бар)
  3.6 (PlaylistsList)
  3.7 (AuthForm)
  3.8 (fetchWithAuth)


================================================================================
5. ПОВНИЙ СПИСОК API ЕНДПОЇНТІВ (для довідки)
================================================================================

AUTH:
  POST   /api/auth/register      { email, password, username }
  POST   /api/auth/login         { email, password }
  POST   /api/auth/verify        { token }
  POST   /api/auth/refresh       { refreshToken }
  GET    /api/auth/google        (редирект)
  GET    /api/auth/google/callback (редирект)

USERS:
  GET    /api/users              [JWT]
  GET    /api/users/user         [JWT]
  GET    /api/users/me           [JWT] → { id, username, avatarUrl, createdAt }
  GET    /api/users/:id          [JWT]
  GET    /api/users/username/:username
  PATCH  /api/users/description  [JWT] { newDescription }
  PATCH  /api/users/update-username [JWT] { newUsername }
  PATCH  /api/users/avatar       [JWT] base64 || file

TRACKS:
  GET    /api/tracks             [JWT]
  GET    /api/tracks/my-tracks   [JWT]
  GET    /api/tracks/liked       [JWT]
  GET    /api/tracks/pagination  [JWT] ?limit=&cursor=
  GET    /api/tracks/:id         [JWT]
  GET    /api/tracks/:id/like-status [JWT]
  GET    /api/tracks/user/:username   (public)
  POST   /api/tracks             [JWT] multipart (audio file + fields)
  PATCH  /api/tracks/:id         [JWT]
  PATCH  /api/tracks/:id/visibility [JWT] { status }
  DELETE /api/tracks/:id         [JWT]
  POST   /api/tracks/:id/like    [JWT]
  DELETE /api/tracks/:id/like    [JWT]

PLAYLISTS:
  GET    /api/playlists          [JWT]
  GET    /api/playlists/public   [JWT]
  GET    /api/playlists/my-playlists [JWT]
  GET    /api/playlists/liked    [JWT]
  GET    /api/playlists/:id      [JWT]
  GET    /api/playlists/:id/like-status [JWT]
  GET    /api/playlists/user/:username   (public)
  POST   /api/playlists          [JWT] { name, genre, description, coverUrl }
  POST   /api/playlists/:id/like [JWT]
  POST   /api/playlists/:id/cover [JWT] file || base64
  PATCH  /api/playlists/:id      [JWT] { name, genre, description }
  PATCH  /api/playlists/:id/visibility [JWT] { status }
  DELETE /api/playlists/:id      [JWT]
  POST   /api/playlists/:playlistId/tracks/:trackId [JWT]
  DELETE /api/playlists/:playlistId/tracks/:trackId [JWT]

LIKES:
  POST   /api/likes/track/:trackId [JWT]
  POST   /api/likes/playlist/:playlistId [JWT]


================================================================================
КІНЕЦЬ ДОКУМЕНТУ
================================================================================
